// ==UserScript==
// @name         NixBypass
// @namespace    http://tampermonkey.net/
// @version      1.0.0 бета
// @description  Nix Bypass — клиентский байпас без API
// @author       A_Q
// @match        *://mega-guy.com/*
// @match        *://*.mega-guy.com/*
// @match        *://loot-link.com/*
// @match        *://*.loot-link.com/*
// @match        *://best-links.org/*
// @match        *://*.best-links.org/*
// @match        *://loot-links.com/*
// @match        *://*.loot-links.com/*
// @match        *://megaspremium.com/*
// @match        *://*.megaspremium.com/*
// @match        *://lootdest.com/*
// @match        *://*.lootdest.com/*
// @match        *://direct-links.net/*
// @match        *://*.direct-links.net/*
// @match        *://risquemega.com/*
// @match        *://*.risquemega.com/*
// @match        *://onlyfriends.club/*
// @match        *://*.onlyfriends.club/*
// @match        *://onepiecered.co/*
// @match        *://*.onepiecered.co/*
// @match        *://multileaks.com/*
// @match        *://*.multileaks.com/*
// @match        *://luvsquad-links.com/*
// @match        *://*.luvsquad-links.com/*
// @match        *://lootdest.org/*
// @match        *://*.lootdest.org/*
// @match        *://free-leaks.com/*
// @match        *://*.free-leaks.com/*
// @match        *://goldmega.online/*
// @match        *://*.goldmega.online/*
// @match        *://realiukzemydre.com/*
// @match        *://*.realiukzemydre.com/*
// @match        *://kmendation.com/*
// @match        *://*.kmendation.com/*
// @match        *://lootlinks.co/*
// @match        *://*.lootlinks.co/*
// @match        *://onlyshare.info/*
// @match        *://*.onlyshare.info/*
// @match        *://nswfbox.com/*
// @match        *://*.nswfbox.com/*
// @match        *://eofmukindwo.com/*
// @match        *://*.eofmukindwo.com/*
// @match        *://op-packs.com/*
// @match        *://*.op-packs.com/*
// @match        *://thedetective.online/*
// @match        *://*.thedetective.online/*
// @match        *://forlinkmeg.com/*
// @match        *://*.forlinkmeg.com/*
// @match        *://thegoatpack.org/*
// @match        *://*.thegoatpack.org/*
// @match        *://of-leaks.xyz/*
// @match        *://*.of-leaks.xyz/*
// @match        *://links.spacebin.in/*
// @match        *://*.links.spacebin.in/*
// @match        *://supermeg.com/*
// @match        *://*.supermeg.com/*
// @match        *://onlyfanessereloaded.com/*
// @match        *://*.onlyfanessereloaded.com/*
// @match        *://fanzleaks.com/*
// @match        *://*.fanzleaks.com/*
// @match        *://cemendemons.com/*
// @match        *://*.cemendemons.com/*
// @match        *://premiummegaz.com/*
// @match        *://*.premiummegaz.com/*
// @match        *://thepremium.online/*
// @match        *://*.thepremium.online/*
// @match        *://pasteebins.com/*
// @match        *://*.pasteebins.com/*
// @match        *://baddiezcentral.com/*
// @match        *://*.baddiezcentral.com/*
// @match        *://megazone.website/*
// @match        *://*.megazone.website/*
// @match        *://leak-pragmatic.com/*
// @match        *://*.leak-pragmatic.com/*
// @match        *://megadumpz.com/*
// @match        *://*.megadumpz.com/*
// @match        *://thhaven.net/*
// @match        *://*.thhaven.net/*
// @match        *://meg4fans.com/*
// @match        *://*.meg4fans.com/*
// @match        *://depravityweb.co/*
// @match        *://*.depravityweb.co/*
// @match        *://discordlink.cc/*
// @match        *://*.discordlink.cc/*
// @match        *://megashub.co/*
// @match        *://*.megashub.co/*
// @match        *://streamergirls.org/*
// @match        *://*.streamergirls.org/*
// @match        *://your-leaks.com/*
// @match        *://*.your-leaks.com/*
// @match        *://onlylinksmegas.xyz/*
// @match        *://*.onlylinksmegas.xyz/*
// @match        *://pypy.spacebin.in/*
// @match        *://*.pypy.spacebin.in/*
// @match        *://pypy.in/*
// @match        *://*.pypy.in/*
// @match        *://luvsquad-links.cmo/*
// @match        *://*.luvsquad-links.cmo/*
// @match        *://beast.net.in/*
// @match        *://*.beast.net.in/*
// @match        *://hook.beast.net.in/*
// @match        *://*.hook.beast.net.in/*
// @match        *://all-fans.online/*
// @match        *://*.all-fans.online/*
// @match        *://fansmega.com/*
// @match        *://*.fansmega.com/*
// @match        *://worldpacks.co/*
// @match        *://*.worldpacks.co/*
// @match        *://night-hub.online/*
// @match        *://*.night-hub.online/*
// @match        *://dailyofleaks.com/*
// @match        *://*.dailyofleaks.com/*
// @match        *://pkofs.com/*
// @match        *://*.pkofs.com/*
// @match        *://offree90.com/*
// @match        *://*.offree90.com/*
// @match        *://megadropz.com/*
// @match        *://*.megadropz.com/*
// @match        *://onlyfunlink.com/*
// @match        *://*.onlyfunlink.com/*
// @match        *://direct-links.org/*
// @match        *://*.direct-links.org/*
// @match        *://leaks4you.com/*
// @match        *://*.leaks4you.com/*
// @match        *://onlylinksmegas.com/*
// @match        *://*.onlylinksmegas.com/*
// @match        *://direct-link.net/*
// @match        *://*.direct-link.net/*
// @match        *://downbadleaks.com/*
// @match        *://*.downbadleaks.com/*
// @match        *://onlyfanscloud.com/*
// @match        *://*.onlyfanscloud.com/*
// @match        *://missleakz.com/*
// @match        *://*.missleakz.com/*
// @match        *://leakszone.online/*
// @match        *://*.leakszone.online/*
// @match        *://links-loot.com/*
// @match        *://*.links-loot.com/*
// @match        *://holyfanslinks.com/*
// @match        *://*.holyfanslinks.com/*
// @match        *://utopianleaks.com/*
// @match        *://*.utopianleaks.com/*
// @match        *://megavip.store/*
// @match        *://*.megavip.store/*
// @match        *://drlinker.com/*
// @match        *://*.drlinker.com/*
// @match        *://baddiesheaven.com/*
// @match        *://*.baddiesheaven.com/*
// @match        *://of4lm-links.com/*
// @match        *://*.of4lm-links.com/*
// @match        *://holedonly.store/*
// @match        *://*.holedonly.store/*
// @match        *://lootlinks.com/*
// @match        *://*.lootlinks.com/*
// @match        *://free-content.pro/*
// @match        *://*.free-content.pro/*
// @match        *://milky-center.com/*
// @match        *://*.milky-center.com/*
// @match        *://megaofs.com/*
// @match        *://*.megaofs.com/*
// @match        *://bleleadersto.com/*
// @match        *://*.bleleadersto.com/*
// @match        *://link-target.org/*
// @match        *://*.link-target.org/*
// @match        *://daughablelea.com/*
// @match        *://*.daughablelea.com/*
// @match        *://mymegalinks.com/*
// @match        *://*.mymegalinks.com/*
// @match        *://heroslut.com/*
// @match        *://*.heroslut.com/*
// @match        *://tonordersitye.com/*
// @match        *://*.tonordersitye.com/*
// @match        *://vip-linknetwork.com/*
// @match        *://*.vip-linknetwork.com/*
// @match        *://birdbiss.com/*
// @match        *://*.birdbiss.com/*
// @match        *://loot-labs.com/*
// @match        *://*.loot-labs.com/*
// @match        *://links.lootlabs.gg/*
// @match        *://*.links.lootlabs.gg/*
// @match        *://lootlabs.com/*
// @match        *://*.lootlabs.com/*
// @match        *://link-hub.net/*
// @match        *://*.link-hub.net/*
// @match        *://locconnect.com/*
// @match        *://*.locconnect.com/*
// @match        *://premiumstashdrop.com/*
// @match        *://*.premiumstashdrop.com/*
// @match        *://lootdest.info/*
// @match        *://*.lootdest.info/*
// @match        *://of-area.com/*
// @match        *://*.of-area.com/*
// @match        *://link-target.net/*
// @match        *://*.link-target.net/*
// @match        *://megalnk.com/*
// @match        *://*.megalnk.com/*
// @match        *://lootlink.org/*
// @match        *://*.lootlink.org/*
// @match        *://nsfwcherry.com/*
// @match        *://*.nsfwcherry.com/*
// @match        *://cherrypacks.online/*
// @match        *://*.cherrypacks.online/*
// @match        *://hotstars-leaks.com/*
// @match        *://*.hotstars-leaks.com/*
// @match        *://onlymega.co/*
// @match        *://*.onlymega.co/*
// @match        *://hanimeturks.com/*
// @match        *://*.hanimeturks.com/*
// @match        *://dailyleakz.com/*
// @match        *://*.dailyleakz.com/*
// @match        *://content-hub.club/*
// @match        *://*.content-hub.club/*
// @match        *://dailyadultmegas.com/*
// @match        *://*.dailyadultmegas.com/*
// @match        *://oui-chu.com/*
// @match        *://*.oui-chu.com/*
// @match        *://megalinks.one/*
// @match        *://*.megalinks.one/*
// @match        *://starleakz.com/*
// @match        *://*.starleakz.com/*
// @match        *://babeslink.click/*
// @match        *://*.babeslink.click/*
// @match        *://ofgirls3x.com/*
// @match        *://*.ofgirls3x.com/*
// @match        *://pnp-drops.me/*
// @match        *://*.pnp-drops.me/*
// @match        *://nsfw-paradise.club/*
// @match        *://*.nsfw-paradise.club/*
// @match        *://key-access.co/*
// @match        *://*.key-access.co/*
// @match        *://leaksmix.com/*
// @match        *://*.leaksmix.com/*
// @match        *://sensual-leaks.com/*
// @match        *://*.sensual-leaks.com/*
// @match        *://ofhub-leaks.com/*
// @match        *://*.ofhub-leaks.com/*
// @match        *://onlyforfan.online/*
// @match        *://*.onlyforfan.online/*
// @match        *://sweetjuice-mega.com/*
// @match        *://*.sweetjuice-mega.com/*
// @match        *://crip-hub.com/*
// @match        *://*.crip-hub.com/*
// @match        *://leaksfreeday.com/*
// @match        *://*.leaksfreeday.com/*
// @match        *://thotshaven.online/*
// @match        *://*.thotshaven.online/*
// @match        *://mega-leaked.com/*
// @match        *://*.mega-leaked.com/*
// @match        *://beautifulgirls.social/*
// @match        *://*.beautifulgirls.social/*
// @match        *://tavernleaks.com/*
// @match        *://*.tavernleaks.com/*
// @match        *://nudeleaksteenz.com/*
// @match        *://*.nudeleaksteenz.com/*
// @match        *://slutywet.com/*
// @match        *://*.slutywet.com/*
// @match        *://leakutopia.site/*
// @match        *://*.leakutopia.site/*
// @match        *://secret-packs.com/*
// @match        *://*.secret-packs.com/*
// @match        *://attiktok22.com/*
// @match        *://*.attiktok22.com/*
// @match        *://pancakes-leaks.com/*
// @match        *://*.pancakes-leaks.com/*
// @match        *://diamond-leaks.com/*
// @match        *://*.diamond-leaks.com/*
// @match        *://lewd-leaks.com/*
// @match        *://*.lewd-leaks.com/*
// @match        *://ftbleaks.net/*
// @match        *://*.ftbleaks.net/*
// @match        *://thepytheaven.org/*
// @match        *://*.thepytheaven.org/*
// @match        *://of-region.online/*
// @match        *://*.of-region.online/*
// @match        *://admiregirls-byme.com/*
// @match        *://*.admiregirls-byme.com/*
// @match        *://mzehoney12.com/*
// @match        *://*.mzehoney12.com/*
// @match        *://herplace.online/*
// @match        *://*.herplace.online/*
// @match        *://megaplugleaks.com/*
// @match        *://*.megaplugleaks.com/*
// @match        *://of-kingdom.com/*
// @match        *://*.of-kingdom.com/*
// @match        *://onlyfansmegafolder.com/*
// @match        *://*.onlyfansmegafolder.com/*
// @match        *://adfoc.us/*
// @match        *://*.adfoc.us/*
// @match        *://boost.ink/*
// @match        *://*.boost.ink/*
// @match        *://cuty.io/*
// @match        *://*.cuty.io/*
// @match        *://cety.app/*
// @match        *://*.cety.app/*
// @match        *://linkvertise.com/*
// @match        *://*.linkvertise.com/*
// @match        *://linkvertise.net/*
// @match        *://*.linkvertise.net/*
// @match        *://linkvertise.me/*
// @match        *://*.linkvertise.me/*
// @match        *://link-to.net/*
// @match        *://*.link-to.net/*
// @match        *://mboost.me/*
// @match        *://*.mboost.me/*
// @match        *://bst.gg/*
// @match        *://*.bst.gg/*
// @match        *://booo.st/*
// @match        *://*.booo.st/*
// @match        *://mendationforc.info/*
// @match        *://*.mendationforc.info/*
// @match        *://paster.so/*
// @match        *://*.paster.so/*
// @match        *://paster.gg/*
// @match        *://*.paster.gg/*
// @match        *://rekonise.com/*
// @match        *://*.rekonise.com/*
// @match        *://social-unlock.com/*
// @match        *://*.social-unlock.com/*
// @match        *://socialwolvez.com/*
// @match        *://*.socialwolvez.com/*
// @match        *://sub2get.com/*
// @match        *://*.sub2get.com/*
// @match        *://sub2unlock.com/*
// @match        *://*.sub2unlock.com/*
// @match        *://sub2unlock.io/*
// @match        *://*.sub2unlock.io/*
// @match        *://sub2unlock.net/*
// @match        *://*.sub2unlock.net/*
// @match        *://sub2unlock.online/*
// @match        *://*.sub2unlock.online/*
// @match        *://sub2unlock.top/*
// @match        *://*.sub2unlock.top/*
// @match        *://sub4unlock.pro/*
// @match        *://*.sub4unlock.pro/*
// @match        *://sub4unlock.com/*
// @match        *://*.sub4unlock.com/*
// @match        *://sub4unlock.io/*
// @match        *://*.sub4unlock.io/*
// @match        *://subfinal.com/*
// @match        *://*.subfinal.com/*
// @match        *://unlocknow.net/*
// @match        *://*.unlocknow.net/*
// @match        *://v.gd/*
// @match        *://*.v.gd/*
// @match        *://work.ink/*
// @match        *://*.work.ink/*
// @match        *://workink.net/*
// @match        *://*.workink.net/*
// @match        *://r.work.ink/*
// @match        *://*.r.work.ink/*
// @match        *://workink.one/*
// @match        *://*.workink.one/*
// @match        *://workink.me/*
// @match        *://*.workink.me/*
// @match        *://ytsubme.com/*
// @match        *://*.ytsubme.com/*
// @match        *://esohasl.net/*
// @match        *://*.esohasl.net/*
// @match        *://rbscripts.net/*
// @match        *://*.rbscripts.net/*
// @match        *://link.rbscripts.net/*
// @match        *://*.link.rbscripts.net/*
// @match        *://gateway.platoboost.com/*
// @match        *://*.gateway.platoboost.com/*
// @match        *://admaven.com/*
// @match        *://*.admaven.com/*
// @match        *://luarmor.net/*
// @match        *://*.luarmor.net/*
// @license      MIT
// @grant        none
// @run-at       document-start
// ==/UserScript==

(function () {
    'use strict';

    const CONFIG = {
        timeout: 30000,
        checkInterval: 600,
        maxRetries: 3,
        autoClickDelay: { min: 150, max: 2000 },
        debug: true
    };

    Object.defineProperty(navigator, 'webdriver', { get: () => undefined });
    window.chrome = { runtime: {}, loadTimes: function(){}, csi: function(){}, app: {} };
    Object.defineProperty(navigator, 'plugins', { get: () => [1, 2, 3, 4, 5] });
    Object.defineProperty(navigator, 'languages', { get: () => ['en-US', 'en'] });
    Object.defineProperty(document, 'hasFocus', { get: () => !document.hidden });
    Object.defineProperty(window, 'outerHeight', { get: () => window.innerHeight });
    Object.defineProperty(window, 'outerWidth', { get: () => window.innerWidth });

    const url = window.location.href;
    const hostname = window.location.hostname.toLowerCase();
    const log = (...a) => CONFIG.debug && console.log('%c[NixBypass]', 'color:#00ff88;font-weight:bold', ...a);
    const err = (...a) => CONFIG.debug && console.error('%c[NixBypass]', 'color:#ff4466;font-weight:bold', ...a);

    log('Запущен на:', url);

    let bypassed = false;
    let intervals = [];
    let observer = null;

    function cleanup() {
        intervals.forEach(clearInterval);
        intervals = [];
        if (observer) observer.disconnect();
    }

    function isUrl(str) {
        return typeof str === 'string' && (str.startsWith('http://') || str.startsWith('https://'));
    }

    function isBase64(str) {
        if (typeof str !== 'string' || str.length < 8) return false;
        const cleaned = str.replace(/-/g, '+').replace(/_/g, '/');
        if (!/^[A-Za-z0-9+/=]+$/.test(cleaned)) return false;
        try { return atob(cleaned).length > 0; } catch { return false; }
    }

    function decodeB64(str) {
        try {
            const c = str.replace(/-/g, '+').replace(/_/g, '/');
            const padded = c + '='.repeat((4 - c.length % 4) % 4);
            return atob(padded);
        } catch { return null; }
    }

    function redirect(target) {
        if (bypassed || !isUrl(target)) return false;
        bypassed = true;
        cleanup();
        log('Редирект ->', target);
        const methods = [
            () => window.location.replace(target),
            () => window.location.href = target,
            () => { const a = document.createElement('a'); a.href = target; a.rel = 'noopener noreferrer'; a.click(); },
            () => window.open(target, '_self')
        ];
        for (const fn of methods) {
            try { fn(); return true; } catch(e) {}
        }
        return false;
    }

    function getParam(name) {
        const p = new URLSearchParams(window.location.search);
        return p.get(name) || p.get(name.toLowerCase());
    }

    function isVisible(el) {
        if (!el) return false;
        const rect = el.getBoundingClientRect();
        return rect.width > 0 && rect.height > 0;
    }

    function isClickable(el) {
        if (!el) return false;
        const style = window.getComputedStyle(el);
        return style.display !== 'none' && style.visibility !== 'hidden' &&
               style.pointerEvents !== 'none' && !el.disabled && isVisible(el);
    }

    function humanClick(el) {
        if (!isClickable(el)) return Promise.resolve(false);
        return new Promise(resolve => {
            el.scrollIntoView({ behavior: 'smooth', block: 'center' });
            el.focus();
            const delay = Math.random() * (CONFIG.autoClickDelay.max - CONFIG.autoClickDelay.min) + CONFIG.autoClickDelay.min;
            setTimeout(() => {
                const evt = new MouseEvent('click', {
                    bubbles: true, cancelable: true, view: window,
                    clientX: Math.random() * 100 + 50, clientY: Math.random() * 100 + 50,
                    screenX: Math.random() * 1000, screenY: Math.random() * 800
                });
                el.dispatchEvent(evt);
                resolve(true);
            }, delay);
        });
    }

    function handleCaptcha() {
        const iframes = document.querySelectorAll('iframe[src*="captcha"], iframe[src*="turnstile"], iframe[src*="recaptcha"], iframe[title*="captcha"], iframe[src*="challenges"]');
        iframes.forEach(iframe => {
            const rect = iframe.getBoundingClientRect();
            if (rect.width > 0 && rect.height > 0) {
                try { iframe.contentWindow.postMessage({ type: 'click' }, '*'); } catch(e) {}
                setTimeout(() => iframe.click(), Math.random() * 300 + 100);
            }
        });

        if (typeof window.turnstile !== 'undefined') {
            const originalRender = window.turnstile.render;
            if (originalRender) {
                window.turnstile.render = function(container, params) {
                    if (params && params.callback) {
                        setTimeout(() => params.callback(), 2000 + Math.random() * 3000);
                    }
                    return originalRender.apply(this, arguments);
                };
            }
            if (window.turnstile.getResponse && !window.turnstile.getResponse()) {
                const containers = document.querySelectorAll('[data-sitekey]');
                containers.forEach(c => {
                    if (!c.hasChildNodes() || c.childElementCount === 0) {
                        try { window.turnstile.render(c); } catch(e) {}
                    }
                });
            }
        }

        if (typeof window.grecaptcha !== 'undefined') {
            try {
                const widgets = document.querySelectorAll('.g-recaptcha');
                widgets.forEach(w => {
                    if (!w.dataset.widgetId) {
                        window.grecaptcha.render(w, { sitekey: w.dataset.sitekey, callback: () => {} });
                    } else {
                        window.grecaptcha.execute(w.dataset.widgetId);
                    }
                });
            } catch(e) {}
        }
    }

    function checkBase64Param() {
        const r = getParam('r');
        if (r && isBase64(r)) {
            const dec = decodeB64(r);
            if (dec && isUrl(dec)) {
                log('Base64 параметр r ->', dec);
                redirect(dec);
                return true;
            }
        }
        const params = new URLSearchParams(window.location.search);
        for (const [k, v] of params) {
            if (v.length > 20 && isBase64(v)) {
                const dec = decodeB64(v);
                if (dec && isUrl(dec)) {
                    log(`Base64 в параметре "${k}" ->`, dec);
                    redirect(dec);
                    return true;
                }
            }
        }
        return false;
    }

    function checkDirectParams() {
        const names = ['link', 'url', 'to', 'redirect', 'go', 'target', 'dest', 'destination', 'return', 'returnurl', 'callback', 'next', 'redir', 'forward', 'continue', 'file', 'page', 'site', 'ref', 'u', 'd', 'dl'];
        for (const n of names) {
            const v = getParam(n);
            if (v && isUrl(v)) {
                log(`Прямой URL в "${n}"`);
                redirect(v);
                return true;
            }
        }
        return false;
    }

    function checkHashParams() {
        const hash = window.location.hash.slice(1);
        if (!hash) return false;
        const hp = new URLSearchParams(hash);
        for (const [k, v] of hp) {
            if (isUrl(v)) {
                log(`URL в hash "${k}"`);
                redirect(v);
                return true;
            }
            if (v.length > 20 && isBase64(v)) {
                const dec = decodeB64(v);
                if (dec && isUrl(dec)) {
                    log(`Base64 в hash "${k}"`);
                    redirect(dec);
                    return true;
                }
            }
        }
        if (hash.length > 20 && isBase64(hash)) {
            const dec = decodeB64(hash);
            if (dec && isUrl(dec)) {
                log('Hash целиком — base64');
                redirect(dec);
                return true;
            }
        }
        return false;
    }

    function checkMetaRefresh() {
        const meta = document.querySelector('meta[http-equiv="refresh" i]');
        if (meta) {
            const content = meta.getAttribute('content') || '';
            const match = content.match(/url=["']?(.+?)["']?$/i);
            if (match && isUrl(match[1].trim())) {
                log('Meta refresh ->', match[1].trim());
                redirect(match[1].trim());
                return true;
            }
        }
        return false;
    }

    function checkWindowVars() {
        const patterns = [
            /window\.(?:_destination|destination|targetUrl|redirect_url|finalUrl|endUrl|realUrl|actualUrl|goUrl)\s*=\s*["']([^"']+)["']/i,
            /var\s+(?:destination|targetUrl|redirect|finalUrl|realUrl|actualUrl)\s*=\s*["']([^"']+)["']/i,
            /(?:destination|redirect|target|final|real|actual)_?url\s*[:=]\s*["']([^"']+)["']/i,
            /href\s*=\s*["'](https?:\/\/[^"']{10,})["']/i,
            /window\.location\s*=\s*["'](https?:\/\/[^"']+)["']/i,
            /window\.location\.href\s*=\s*["'](https?:\/\/[^"']+)["']/i
        ];
        const scripts = document.querySelectorAll('script:not([src])');
        for (const s of scripts) {
            const text = s.textContent || '';
            for (const p of patterns) {
                const m = text.match(p);
                if (m && isUrl(m[1])) {
                    log('URL в inline-JS');
                    redirect(m[1]);
                    return true;
                }
            }
        }
        return false;
    }

    function checkPageLinks() {
        const skip = ['loot-link', 'lootlink', 'platoboost', 'greasyfork', 'tampermonkey', 'discord', 'telegram', 'twitter', 'facebook', 'instagram', 'rekonise', 'linkvertise', 'workink', 'paster', 'boost', 'adfoc', 'admaven', 'mendationforc', 'luarmor', 'cuty', 'cety', 'sub2', 'sub4', 'social', 'unlock', 'mboost', 'bst', 'booo', 'v.gd', 'esohasl', 'rbscripts'];
        const links = document.querySelectorAll('a[href]');
        for (const link of links) {
            const href = link.href;
            if (!href || !isUrl(href) || href === url) continue;
            if (skip.some(s => href.toLowerCase().includes(s))) continue;
            const isExternal = !href.includes(window.location.hostname);
            if (isExternal && href.length > 20) {
                log('Найдена внешняя ссылка ->', href);
                redirect(href);
                return true;
            }
        }
        return false;
    }

    function checkDataAttrs() {
        const els = document.querySelectorAll('[data-url], [data-href], [data-redirect], [data-link], [data-destination], [data-target]');
        for (const el of els) {
            for (const attr of ['data-url', 'data-href', 'data-redirect', 'data-link', 'data-destination', 'data-target']) {
                const v = el.getAttribute(attr);
                if (v && isUrl(v)) {
                    log(`URL в ${attr}`);
                    redirect(v);
                    return true;
                }
                if (v && v.length > 20 && isBase64(v)) {
                    const dec = decodeB64(v);
                    if (dec && isUrl(dec)) {
                        log(`Base64 в ${attr}`);
                        redirect(dec);
                        return true;
                    }
                }
            }
        }
        return false;
    }

    function checkFetchIntercept() {
        const originalFetch = window.fetch;
        window.fetch = async function(...args) {
            const response = await originalFetch.apply(this, args);
            try {
                const clone = response.clone();
                const text = await clone.text();
                const urlMatch = text.match(/"(https?:\/\/[^"]{20,})"/);
                if (urlMatch && isUrl(urlMatch[1])) {
                    log('URL в fetch-ответе');
                    redirect(urlMatch[1]);
                }
            } catch(e) {}
            return response;
        };
    }

    function checkPlatoboost() {
        if (!hostname.includes('platoboost')) return false;
        const selectors = 'button, a, [role="button"], input[type="button"], input[type="submit"]';
        const buttons = document.querySelectorAll(selectors);
        const keywords = ['continue', 'get link', 'getlink', 'proceed', 'next', 'skip', 'claim', 'go', 'finish', 'verify', 'complete', 'download'];
        for (const btn of buttons) {
            if (!isClickable(btn)) continue;
            const text = (btn.textContent || '').toLowerCase().trim();
            const id = (btn.id || '').toLowerCase();
            const cls = (btn.className || '').toString().toLowerCase();
            const match = keywords.some(kw => text.includes(kw)) || id.includes('continue') || id.includes('get') || id.includes('next') || id.includes('skip');
            const skipBtn = cls.includes('footer') || cls.includes('social') || cls.includes('ad') || text.includes('advertisement');
            if (match && !skipBtn) {
                log('Клик: Platoboost кнопка');
                humanClick(btn);
                return true;
            }
        }
        return false;
    }

    function checkLinkvertise() {
        if (!hostname.includes('linkvertise') && !hostname.includes('link-to')) return false;
        const path = window.location.pathname;
        const match = path.match(/\/(\d+)\/(.+)/);
        if (match) {
            const id = match[1];
            const slug = match[2];
            const apiUrl = `https://publisher.linkvertise.com/api/v1/redirect/link/static/${id}/${slug}`;
            log('Linkvertise API запрос:', apiUrl);
            fetch(apiUrl)
                .then(r => r.json())
                .then(data => {
                    if (data && data.data && data.data.link && data.data.link.id) {
                        const linkId = data.data.link.id;
                        const payload = btoa(JSON.stringify({ timestamp: Math.floor(Date.now() / 1000), random: "6548307", link_id: linkId }));
                        const targetUrl = `https://publisher.linkvertise.com/api/v1/redirect/link/${id}/${slug}/target?serial=${payload}`;
                        return fetch(targetUrl);
                    }
                    throw new Error('No link ID');
                })
                .then(r => r.json())
                .then(data => {
                    if (data && data.data && data.data.link && data.data.link.target) {
                        log('Linkvertise цель найдена');
                        redirect(data.data.link.target);
                    }
                })
                .catch(e => err('Linkvertise bypass error:', e));
            return true;
        }
        return false;
    }

    function checkWorkInk() {
        if (!hostname.includes('work') && !hostname.includes('workink')) return false;
        const path = window.location.pathname;
        const match = path.match(/\/(\w+)\/?/);
        if (match) {
            log('Work.ink обнаружен, поиск цели...');
            setTimeout(() => {
                const scripts = document.querySelectorAll('script:not([src])');
                for (const s of scripts) {
                    const text = s.textContent || '';
                    const destMatch = text.match(/"(https?:\/\/[^"]{20,})"/);
                    if (destMatch && isUrl(destMatch[1])) {
                        redirect(destMatch[1]);
                        return;
                    }
                }
            }, 1500);
            return true;
        }
        return false;
    }

    function checkAdfoc() {
        if (!hostname.includes('adfoc')) return false;
        const scripts = document.querySelectorAll('script:not([src])');
        for (const s of scripts) {
            const text = s.textContent || '';
            const match = text.match(/var\s+click_url\s*=\s*["'](https?:\/\/[^"']+)["']/i);
            if (match && isUrl(match[1])) {
                log('Adfoc.us click_url найден');
                redirect(match[1]);
                return true;
            }
        }
        return false;
    }

    function checkBoostInk() {
        if (!hostname.includes('boost')) return false;
        const scripts = document.querySelectorAll('script:not([src])');
        for (const s of scripts) {
            const text = s.textContent || '';
            const match = text.match(/window\.location\.href\s*=\s*["'](https?:\/\/[^"']+)["']/i);
            if (match && isUrl(match[1])) {
                log('Boost.ink редирект найден');
                redirect(match[1]);
                return true;
            }
        }
        return false;
    }

    function checkPaster() {
        if (!hostname.includes('paster')) return false;
        const scripts = document.querySelectorAll('script:not([src])');
        for (const s of scripts) {
            const text = s.textContent || '';
            const match = text.match(/"(https?:\/\/[^"]{20,})"/);
            if (match && isUrl(match[1]) && !match[1].includes('paster')) {
                log('Paster цель найдена');
                redirect(match[1]);
                return true;
            }
        }
        return false;
    }

    function checkRekonise() {
        if (!hostname.includes('rekonise')) return false;
        const scripts = document.querySelectorAll('script:not([src])');
        for (const s of scripts) {
            const text = s.textContent || '';
            const match = text.match(/"(https?:\/\/[^"]{20,})"/);
            if (match && isUrl(match[1]) && !match[1].includes('rekonise')) {
                log('Rekonise цель найдена');
                redirect(match[1]);
                return true;
            }
        }
        return false;
    }

    function checkCutyCety() {
        if (!hostname.includes('cuty') && !hostname.includes('cety')) return false;
        const hash = window.location.hash.slice(1);
        if (hash && isUrl(hash)) {
            log('Cuty/Cety hash URL');
            redirect(hash);
            return true;
        }
        const scripts = document.querySelectorAll('script:not([src])');
        for (const s of scripts) {
            const text = s.textContent || '';
            const match = text.match(/window\.location\.replace\(["'](https?:\/\/[^"']+)["']\)/i);
            if (match && isUrl(match[1])) {
                redirect(match[1]);
                return true;
            }
        }
        return false;
    }

    function checkMendationforc() {
        if (!hostname.includes('mendationforc')) return false;
        const scripts = document.querySelectorAll('script:not([src])');
        for (const s of scripts) {
            const text = s.textContent || '';
            const match = text.match(/"(https?:\/\/[^"]{20,})"/);
            if (match && isUrl(match[1]) && !match[1].includes('mendationforc')) {
                log('Mendationforc цель найдена');
                redirect(match[1]);
                return true;
            }
        }
        return false;
    }

    function checkLuarmor() {
        if (!hostname.includes('luarmor')) return false;
        const scripts = document.querySelectorAll('script:not([src])');
        for (const s of scripts) {
            const text = s.textContent || '';
            const match = text.match(/"(https?:\/\/[^"]{20,})"/);
            if (match && isUrl(match[1]) && !match[1].includes('luarmor')) {
                log('Luarmor цель найдена');
                redirect(match[1]);
                return true;
            }
        }
        const params = new URLSearchParams(window.location.search);
        for (const [k, v] of params) {
            if (isUrl(v) && !v.includes('luarmor')) {
                redirect(v);
                return true;
            }
        }
        return false;
    }

    function checkLootlabs() {
        if (!hostname.includes('lootlabs') && !hostname.includes('lootlinks') && !hostname.includes('free-content') && !hostname.includes('admaven')) return false;
        if (checkBase64Param()) return true;
        if (checkDirectParams()) return true;
        if (checkHashParams()) return true;
        if (checkMetaRefresh()) return true;
        if (checkWindowVars()) return true;
        if (checkDataAttrs()) return true;
        if (checkPageLinks()) return true;
        return false;
    }

    function tryBypass() {
        if (bypassed) return;
        handleCaptcha();
        if (checkPlatoboost()) return;
        if (checkLinkvertise()) return;
        if (checkWorkInk()) return;
        if (checkAdfoc()) return;
        if (checkBoostInk()) return;
        if (checkPaster()) return;
        if (checkRekonise()) return;
        if (checkCutyCety()) return;
        if (checkMendationforc()) return;
        if (checkLuarmor()) return;
        if (checkLootlabs()) return;
        if (checkBase64Param()) return;
        if (checkDirectParams()) return;
        if (checkHashParams()) return;
        if (checkMetaRefresh()) return;
        if (checkWindowVars()) return;
        if (checkDataAttrs()) return;
        if (checkPageLinks()) return;
    }

    checkFetchIntercept();
    tryBypass();

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', tryBypass);
    }

    window.addEventListener('load', tryBypass);

    const interval = setInterval(() => {
        if (!document.hidden && !bypassed) tryBypass();
    }, CONFIG.checkInterval);
    intervals.push(interval);

    function startObserver() {
        if (!document.body) return setTimeout(startObserver, 50);
        observer = new MutationObserver(() => {
            if (!bypassed) tryBypass();
        });
        observer.observe(document.body, {
            childList: true,
            subtree: true,
            attributes: true,
            attributeFilter: ['href', 'src', 'data-url', 'data-href', 'data-redirect', 'data-link', 'data-destination']
        });
    }

    if (document.body) {
        startObserver();
    } else {
        document.addEventListener('DOMContentLoaded', startObserver);
    }

    setTimeout(() => {
        if (!bypassed) {
            cleanup();
            log('Таймаут — назначение не обнаружено');
        }
    }, CONFIG.timeout);

})();
